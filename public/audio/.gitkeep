Place the music here as: music.mp3

It starts on the guest's tap to open the envelope — that gesture is what lets a
browser begin audio at all — and the floating control takes over from there.
There is no second click to start it.

While this file is absent the music control does not render, and the envelope
opens silently. Nothing else changes.
